# APIViewService client library for JavaScript



Key links:

- [Package (NPM)](https://www.npmjs.com/package/APIViewService)

## Getting started

### Currently supported environments

- LTS versions of Node.js

### Install the `APIViewService` package

Install the APIViewService client library for JavaScript with `npm`:

```bash
npm install APIViewService
```
